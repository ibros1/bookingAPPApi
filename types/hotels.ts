export interface iCreatedHotel {
  name: string;
  addressId: string;
  bookerId?: string;
}
export interface iUpdatedHotel {
  id: string;
  name: string;
  addressId: string;
  bookerId?: string;
}
